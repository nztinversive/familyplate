// Convex Auth Configuration
// TODO: Set up Convex Auth with Resend for magic link
// For now, this is a placeholder config

export default {
  providers: [
    // Will configure:
    // 1. Email/Password (immediate)
    // 2. Magic Link via Resend (production)
  ],
};
